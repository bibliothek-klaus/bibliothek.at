@model List<bibliothek.at.Models.MediaItem>
@{
    ViewBag.Title = "Neuanschaffungen";
}

<h2>Neuanschaffungen</h2>

<div class="row equal">
    @{
        var lastMediaArt = string.Empty;
        foreach (var item in Model)
        {
            if (lastMediaArt != item.MedienArt)
            {
                lastMediaArt = item.MedienArt;
                <div class="col-xs-12"><h3>Medienart: @item.MedienArt</h3></div>
            }

            <div class="col-xs-12 col-sm-4 col-md-3">
                <div class="thumbnail">
                    <a href="/Medien/Detail/@item.Id">
                        <div class="w-100" style="height:300px; display:block; background-image:url('@item.ImageUrl'); background-repeat:no-repeat; background-size:100%;"></div>
                    </a>
                    <div class="caption text-center">
                        <a href="/Medien/Detail/@item.Id"><h4>@item.Sachtitel <small>@item.Verfasser</small></h4></a>
                    </div>
                </div>
            </div>
        }
    }

</div>

@*<table class="table table-bordered">
    <tr>
        <th></th>
        <th class="hidden-xs">Mediennummer</th>
        <th class="hidden-xs">Art</th>
        <th class="hidden-xs">Systematik</th>
        <th class="hidden-xs">Sachtitel</th>
        <th class="hidden-xs">Verfügbar</th>
        <th class="hidden-xs">Verfasser</th>
        <th class="visible-xs">Medium</th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td><img src="@item.ImageUrl" class="img-responsive" width="100" /></td>
            <td class="hidden-xs">@item.Id</td>
            <td class="hidden-xs">@item.MedienArt</td>
            <td class="hidden-xs">@item.Systematik</td>
            <td class="hidden-xs">@item.Sachtitel</td>
            <td class="hidden-xs">
            @if (item.Status)
            { <div style="width:10px; height:10px; border-radius:5px; background-color:green;"></div> }
            else
            { <div style="width:10px; height:10px; border-radius:5px; background-color:red;"></div> }
            </td>
            <td class="hidden-xs">@item.Verfasser</td>
            <td class="visible-xs">
                @item.Id<br />
                <strong>@item.Sachtitel</strong><br />
                @item.Verfasser
            </td>
            <td><a class="btn btn-primary btn-xs" href="/Medien/Detail/@item.Id">Details</a></td>
        </tr>
    }
</table>*@

@section scripts {
<script type="text/javascript">
    $("h4").fitText(1.2);
</script>
}